# 🔐 SecurePass Generator

A modular, **Object-Oriented (OOP)** Python application that generates secure passwords through a sleek **Streamlit** web interface. This project transforms a basic script into a scalable, user-friendly tool.

## ✨ Features
 - **Standard Random:** Customizable length with toggles for numbers and symbols.

 - **Memorable Phrases:** Generates easy-to-remember passwords using a vocabulary list and custom separators.

 - **PIN Code:** Generates numeric pins of any length.

 - **Modern UI:** A reactive web interface with "click-to-copy" functionality.

 - **Modular Design:** Logic is separated from the UI, making it easy to integrate into other projects.

